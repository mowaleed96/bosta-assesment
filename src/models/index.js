const TokenModel = require('./token');
const UserModel = require('./user');
const CheckModel = require('./check');
const ReportModel = require('./report');

module.exports = {
  TokenModel, UserModel, CheckModel, ReportModel,
};
